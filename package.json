{
  "name": "personal-running-coach",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev:web",
    "dev:web": "npm run dev --workspace @personal-running-coach/web",
    "dev:worker": "npm run dev --workspace @personal-running-coach/worker",
    "build": "npm run build --workspaces --if-present --include-workspace-root=false",
    "lint": "npm run lint --workspaces --if-present --include-workspace-root=false",
    "typecheck": "npm run typecheck --workspaces --if-present --include-workspace-root=false",
    "check": "npm run lint && npm run typecheck",
    "issues:deferred": "tsx scripts/create-deferred-issues.ts"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.7.2",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  }
}
